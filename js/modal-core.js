// js/modal-core.js - Med collapsible sektioner (v2.00.25)

function showPoliticianModal(politicianId) {
  // ... existing code ...
}

// Make sure it's available globally
window.showPoliticianModal = showPoliticianModal;